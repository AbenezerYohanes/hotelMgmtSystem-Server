#!/usr/bin/env node
// Script used by package.json to indicate archived MySQL utilities.
console.error('Archived: MySQL-related scripts were removed after migration to MongoDB.');
console.error('If you need to run migrations, use MongoDB seed/migration scripts instead.');
process.exit(1);
